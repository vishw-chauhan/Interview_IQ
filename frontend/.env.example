# Leave empty during development (Vite proxies /api to the backend).
# In production, set this to the deployed backend URL, for example:
# VITE_API_URL=https://your-backend.example.com/api
VITE_API_URL=